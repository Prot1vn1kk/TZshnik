# 🚀 БЫСТРЫЙ СТАРТ

## Windows: Нажимайте на этот файл
```
run_bot.bat
```

## Linux/Mac: В терминале
```bash
chmod +x run_bot.sh
./run_bot.sh
```

## Что происходит:
1. ✅ Создаёт виртуальное окружение (если его нет)
2. ✅ Устанавливает зависимости из requirements.txt
3. ✅ Запускает бота

## Если есть ошибка:

### Вариант 1: Вручную (Windows)
```powershell
cd TelegramBot_v2
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python -m bot.main
```

### Вариант 2: Вручную (Linux/Mac)
```bash
cd TelegramBot_v2
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python -m bot.main
```

## Как остановить бота:
- Нажмите **Ctrl+C** в терминале

---

**Бот будет слушать сообщения и отвечать на них!**

Логи будут выводиться в терминал.

Все файлы БД и экспорты сохраняются в папке `data/` и `exports/`
