# 🐳 ТЗшник в Docker - Команды управления

## ✅ Бот запущен и работает!

### 📊 Проверка статуса
```powershell
docker ps                        # Список запущенных контейнеров
docker-compose logs --tail=20    # Последние 20 строк логов
docker-compose logs -f           # Логи в реальном времени (Ctrl+C для выхода)
```

### 🔄 Управление
```powershell
docker-compose up -d             # Запустить бота в фоне
docker-compose down              # Остановить бота
docker-compose restart          # Перезапустить бота
```

### 🛠️ Техническое обслуживание
```powershell
# Обновление бота (после изменения кода)
docker-compose down
docker build -t tzshnik-bot .
docker-compose up -d

# Просмотр использования ресурсов
docker stats tzshnik-bot

# Подключение к контейнеру (для отладки)
docker exec -it tzshnik-bot /bin/bash
```

### 📁 Файлы и папки
- `data/` - База данных SQLite (автоматически создаётся)
- `exports/` - Экспортированные PDF файлы
- `TelegramBot_v2/.env` - Конфигурация (токены API)

### 🔧 Если нужно изменить настройки:
1. Отредактируйте `TelegramBot_v2\.env`
2. Перезапустите: `docker-compose restart`

### 🆘 Проблемы и решения

**Бот не отвечает на сообщения:**
```powershell
docker-compose logs --tail=50    # Смотрите логи на ошибки
docker-compose restart          # Перезапустите
```

**Ошибка "port already in use":**
```powershell
docker-compose down              # Остановите все контейнеры
docker ps -a                    # Проверьте что всё остановлено
```

**Не хватает места на диске:**
```powershell
docker system prune -a          # Очистите неиспользуемые образы
```

### 📈 Мониторинг
- Логи сохраняются автоматически Docker'ом
- Контейнер автоматически перезапускается при падении (`restart: unless-stopped`)
- Health check следит за работоспособностью

---

## 🎯 Бот готов к работе!

Он работает в фоновом режиме и будет автоматически запускаться при перезагрузке компьютера.

Все данные сохраняются в папках `data/` и `exports/` на вашем компьютере.